<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Enable component scanning for the specified base package(s) -->
    <context:component-scan base-package="com.example.Controller" />

    <!-- Configure annotation-driven MVC controllers -->
    <mvc:annotation-driven />

    <!-- Define the view resolver for JSP views -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!--
    Configure DataDirect Technologies (DDT) DataSource
    Note: Uncomment and customize this section if you are using DDT DataSource
    -->
    <!--
    <bean id="dataSource" class="com.ddtek.jdbcx.oracle.OracleDataSource">
        <property name="user" value="your_username" />
        <property name="password" value="your_password" />
        <property name="url" value="jdbc:datadirect:oracle://your_database_url" />
    </bean>
    -->

    <!--
    Configure Transaction Manager
    Note: Uncomment and customize this section if you need transaction management
    -->
    <!--
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>
    -->

</beans>
